# 2026-09-25 工作日志

## P0-1 修复（报告数据月份断档）

- `agent/tools/agents_tools.py` 的 `fetch_external_data`：请求月份无记录时回退到语义上
  最接近的有数据月份（早于数据→最早；晚于数据→最晚；区间内空洞→不晚于请求月的最近月），
  返回 JSON 中带 `实际月份`/`用户请求月份`/`数据说明` 字段；用户完全无记录仍返回空字符串。
  工具 description 同步更新，告知模型回退语义。
- `prompts/report_prompt.txt` 输出要求新增第 3 条：返回含"实际月份"时报告开头必须声明
  "以下报告基于 XXXX-XX 的使用数据"。
- 验证脚本 `_verify_p01.py`（根目录，可复跑），4 用例全过：未来月→2025-12、
  正常命中→无回退字段、早于数据→2025-01、用户不存在→空字符串。
- 注意：`@tool` 装饰后必须用 `.invoke({...})` 调用，不能直接函数调用。

## RAG 知识库入库 batch size 超限修复

- 现象：`[加载知识库]xxx 加载失败：status_code: 400 ... batch size is invalid, it should not be larger than 20`
  （用户报告"检索不到知识库的 pdf 文档"）。
- 根因：`Chroma.add_documents(split_document)` 一次性调用 `embed_documents(texts)`，
  DashScope `text-embedding-v3` 单次上限 20 条；`chunk_size=200` 下 6 个知识文件
  切分后**全部 >20 片**（最少 33、最多 100 片），PDF 是 33 片，必触发 400。
  且 `save_md5_hex` 在 `add_documents` 成功后才写 → 失败不落盘 → 每次启动重试报错。
- 修复：`rag/vector_store.py` 新增 `_add_documents_in_batches(documents, batch_size=20)`，
  把 `add_documents` 改成分批调用（每 20 片一次）。
- 验证：`python rag/vector_store.py` 6 个文件全部"内容加载成功"，`md5.txt` 6 行落盘，
  `_collection.count()==335`，检索"迷路"召回 3 条。库文件从 188KB 涨到 2.8MB。
- 注意：`rag/vector_store.py` 直接运行会 `ModuleNotFoundError: No module named 'model'`，
  因为脚本 `if __name__` 没有把项目根加进 sys.path；用 `-c "sys.path.insert(0,os.getcwd())"` 方式跑更稳。
- 另：embedding 的 403（Free quota exhausted）是账号配额问题，与本次 400 是两码事；
  403 需用户去百炼控制台解除"仅免费额度"限制，代码无法自愈。

## 项目代码审计（全量源码 + 日志 + 数据）

产出：`D:\Agent\代码审计与优化建议.md`

### 新增确认的 P0 缺陷（含实测证据）

1. **报告数据与系统时间脱节**（实测坐实）：`records.csv` 只有 2025-01~2025-12，
   而 `get_current_month()` 返回 `2026-09` → `fetch_external_data("1001","2026-09")` 返回 `""`。
   报告功能在当前时间下必然无数据。
2. **报告模式双源分裂**（实测坐实）：`conversations.mode`（DB）与
   `middleware._context_store[user_id]`（进程内存）各写各的；
   `_get_system_prompt(user_id)` 读的是中间件 → 进程重启后历史报告会话用错提示词，
   且 `chat()` 结尾的 `finish_report` 会被误触发。
3. **报告模式"一轮即退"**：日志显示"生成我的使用报告"第 2 轮就收尾（回答长度 9），
   `fetch_external_data` 从未被调用。根因：`report_prompt.txt` 开头写"根据查询回来的信息"
   导致模型误以为数据已就绪；且 `finish_report` 只要某轮无 tool_calls 就退出。
4. **曾出现空工具名/空 query**：`agent_20260917.log` 有大量 `调用工具:  | 参数: {}`
   与 `validation error for rag_summarize (query Field required)`，导致 5 轮全空转。
   累加 chunk 的主路径已修，但 `_execute_tool` 与 `rag_summarize` 缺防御。
5. **`get_weather` 的 `lang_zh` 为空列表时 IndexError**（实测坐实）：
   `current.get("lang_zh", [{}])[0]` —— get 的默认值只在 key 缺失时生效。

### 其他确认项

- `app.py:40-47` 的 `pending_switch_user` 是死代码（全项目 grep：从未被写入）。
- `app.py:241` 注释"输入提交后会自动 rerun"是错的 —— chat_input 提交本身就是一次 rerun，
  之后不会再 rerun，导致侧边栏模式指示器滞后一轮。
- git 仓库处于未解决合并冲突：`README.md` 为 `AA`（stage 2/3 都在）。
- `config/rag.local.yml` 的 `chat_model_name: qwen3.8-max` 建议核实是否有效。

### 验证方法（可复用）

写一次性脚本 → 用**系统 Python 3.11**（`C:\Users\21301\AppData\Local\Programs\Python\Python311\python.exe`）
运行 → `json.dump` 到文件 → Read 读取。注意：模块级 `@tool` 装饰后的对象是
`StructuredTool`，**不能直接调用**，必须 `.invoke({})`（例如 `get_current_month.invoke({})`）。

---

## RAG 检索失败二次定位：hnsw segment reader（用户仍报"检索不到知识库"）

上一节修的是 embedding 批量 400，但用户反馈问题仍在。本次定位到**另一个独立故障**。

### 现象（日志实录）

```
[ReactAgent] 工具 rag_summarize 执行失败: Error executing plan:
Internal error: Error creating hnsw segment reader: Nothing found on disk
  ← chromadb.errors.InternalError，抛在 retriever.invoke() 内部
```

`_execute_tool` 捕获后返回"工具执行失败…请勿重复调用"，模型只能放弃 RAG → 用户看到
"检索不到知识库"。11:21:37、11:29:24 两次提问均如此，耗时仅 ~0.2s（本地立即失败，非超时）。

### 关键结论：磁盘索引本身是好的，属于运行时冲突

逐层排除（每一步都实测）：

| 检查项 | 结论 |
|---|---|
| `md5.txt` 6 行 vs 6 个数据文件 md5 | 完全一致，PDF 已入库 |
| `chroma.sqlite3` embeddings 表 | 335 条，`min=1,max=335,distinct=335` 无空洞 |
| 按 source 分布 | 100+53+53+48+48+**33(PDF)** = 335，无重复无缺失 |
| PDF 解析 | 8 页 / 5105 字；`PyPDFLoader` 正常，非扫描件 |
| PDF 分片 | 33 片，无空 chunk（`empty_chunks=0`）；`chunk_size=200` 下 PDF 独占 33 片 |
| `collection.count()` / `get()` | 335 / 335 |
| k=40 检索召回分布 | 命中"选购指南.txt"(seq 288-335，**最后一个入库的文件**) → **索引完整** |
| 5 个 PDF 专属问题检索 | **全部命中 PDF 且 Top-1** |

→ 所以"PDF 内容检索不到"不是数据问题，而是**每次提问时 Chroma 客户端初始化 hnsw reader 失败**。

### 根因

`RagSummarizeService.__init__` 里同步调用 `self._ensure_knowledge_loaded()`
（全量/增量入库 = **写** Chroma），而该构造发生在**用户首次提问**时（`agents_tools._get_rag()` 懒加载）。
Chroma 是单进程嵌入式向量库，**写与读并发 / 向量库被外部重建后旧句柄失效**，就会在
`retriever.invoke()` 抛 `Nothing found on disk`。

日志时间线佐证：11:10 首次提问触发入库→embedding 400 全挂；11:18 再次入库（6 文件全部
"内容加载成功"，说明 md5.txt 当时是空的）→ 期间另有进程写库；11:21 检索即报 hnsw 错误。

### 修复（3 处，均为外科改动）

1. `rag/rag_service.py::retriever_docs` —— **失败自愈**：捕获异常后 `VectorStoreService()`
   重建客户端再试一次（磁盘索引本来就好的，换句柄即可恢复），仍失败才向上抛。
2. `rag/vector_store.py::_add_documents_in_batches` —— `batch_size` 20 → **10**
   （阿里云 text-embedding 单次硬上限 20，留余量）。
3. `app.py` —— 新增 `@st.cache_resource warm_up_knowledge_base()` 并**在页面渲染前调用**，
   把"首次全量入库"从用户请求路径挪到应用启动阶段（同进程仅执行一次）；
   预热失败不阻断页面，提问时仍走懒加载兜底。

### 验证

端到端 `RagSummarizeService().rag_summarize(...)` 双用例通过：

- "HEPA滤网要多久换一次" → 召回 `[PDF, PDF, 维护保养.txt]`，答"建议每3-6个月更换一次；水洗会降低过滤效果"
- "如何设置最高效的清扫计划" → 召回 `[PDF, 100问2, 100问2]`，答案完整

### 附带事实修正

- 当前 `config/` 下**只有** `agent.yml` / `chroma.yml` / `prompts.yml` / `rag.yml`，
  **不存在 `rag.local.yml`**；`rag.yml` 里就是真实端点（私有 MaaS 的 `compatible-mode/v1` 地址）。
  上文"改 rag.local.yml"的说法与当时现状不符，以本节为准。
  > 后续更新（同日 16:50）：已按设计意图纠正回「`rag.yml` 占位模板 + `rag.local.yml` 存真实值」，
  > 并恢复 `config/rag.yml.example`。此处不再记录端点明文，避免工作记忆成为泄露源。
- hnsw 文件大小不能直接反推向量条数：`header.bin` 里的 `100` 是**预分配容量**，
  不是实际条数（实测 `count()=335` 而 `data_level0.bin` 只有 423600B = 4236×100）。
  **判断索引是否完整要看 `collection.count()` 和 k 大值检索能否召回尾部文件**，不要看文件大小。

### 复现/诊断脚本套路（可复用）

1. 查 sqlite：`select * from embeddings / max_seq_id / segments`（用 `sqlite3` 只读，不碰 Chroma 客户端）
2. 查 source 分布：`embedding_metadata` 表 `key='source'` 分组计数，与 md5.txt 文件数对照
3. 验证索引完整性：`collection.count()` + `similarity_search(k=40)` 看能否召回**最后入库**的文件
4. 以上都健康 → 问题必在**运行时**（句柄/并发），而不是数据

---

## 报告模式双源改造：session["mode"] 成为唯一权威源

### 问题（用户交办）

`conversations.mode`（DB，按 conversation_id）与 `middleware._context_store[user_id]`
（进程内存，按 user_id）各写各的，而 `_get_system_prompt(user_id)` → `_get_mode(user_id)`
读的是**中间件**那一套。两个后果：进程重启后内存上下文丢失 → 历史里的报告会话被套普通
客服提示词；同一用户开两个会话 → 按 user_id 共享状态，互相干扰。

### 改动

| 文件 | 改动 |
|---|---|
| `agent/react_agent.py` | `_get_mode(user_id)` → **公开的 `get_mode(conversation_id)`**，读 `_init_session(cid)["mode"]`；`_get_system_prompt` 改收 `conversation_id`；`chat()`/`chat_stream()` 两处日志与两处提示词组装改用会话 id；`_switch_to_report_mode` 删掉 `elif self.middleware_url: pass` 死分支，注释标明中间件仅为镜像 |
| `app.py` | 侧边栏模式指示器不再读 `middleware.get_context(user_id)`，改为查 `session_store.get_conversation(current_conv_id)["mode"]`（跨用户时按普通模式展示）；「退出报告模式」按钮改调 `get_agent(middleware_url).finish_report(user_id, _cid)` |
| `agent/tools/middleware.py` | **仅注释+文档**：文件头、`get_context`、`is_report_mode` 均标明它是进程内存镜像、非权威源（函数签名与行为未动） |

**补的既有漏洞（我的改动会放大它，必须一起修）**：`chat()`/`chat_stream()` 在"达到最大
迭代次数"的收尾分支原先不复位模式。以前用户还能靠重启进程清掉中间件内存逃脱；改后模式
持久化在 `conversations.mode`，不复位就会**永久卡在报告提示词**。已在两个分支补
`if session["mode"] == "report": self.finish_report(...)`。

模型调用失败的分支**故意不复位**：报告流程中断后用户重试应继续用报告提示词。

### 验证：`_verify_mode.py`（根目录，桩模型，不调 API、不写字库）

`ra.get_chat_model = lambda: FakeModel()` 打桩 + `session_db_path` 指向临时库。5 场景全过：

- s1 多会话隔离：A 进 report 后 `get_mode(A)="report"`/报告提示词，`get_mode(B)="normal"`/普通提示词 ✓
- s2 重启恢复：清空 `middleware._context_store` 后新建实例，A 仍 report 且用报告提示词，B 仍 normal ✓
- s3 作用域：两条会话都置 report，`finish_report(user, A)` 后 A=normal、B 仍 report（内存与 DB 一致）✓
- s4 报告正常产出后自动复位 → normal ✓
- s5 达到最大迭代次数后复位 → normal ✓

回归：`test_react_integration.py` 16 项、`test_session_persist.py` 24 项 **全部通过**；
`py_compile` 四个改动文件通过。

### 注意点

- 桩模型的 `chat()` 路径要求 `bind_tools()` 返回 self、`invoke()` 返回 `AIMessage`（带
  `tool_calls` 时用 `{"name","args","id","type"}`），不要用 `GenericFakeChatModel`（它不产
  tool_calls，进不了报告模式）。
- `app.py` 侧边栏读到的是**上一轮**的 `current_conv_id`（侧边栏先于主区域执行，主区域
  才做 `start_new_conversation`），因此加了 `current_conv_user == user_id` 判断，跨用户时
  直接按普通模式展示。
- 中间件「HTTP 服务」模式下，agent 进程调的仍是**本进程**的 `middleware.fill_context`，
  HTTP 服务进程里的 `_context_store` 不会被更新。因中间件已非权威源，不影响功能。

---

## 网页端检验手段补齐（配合上一条改造）

### 为了让「双源」在页面上肉眼可见，新增两处 UI

| 位置 | 改动 |
|---|---|
| `app.py` 侧边栏 | 新增折叠面板「🔍 状态自检（调试）」：并排显示 `权威模式（conversations.mode）` / `中间件镜像（user_id 进程内存）`，不一致时给 warning，并提示本该用哪套提示词 |
| `app.py` 对话收尾 | 补上 `st.rerun()`。侧边栏代码先于主区域执行，而本轮的模式变更（报告自动复位、落盘）发生在侧边栏渲染之后 → **不 rerun 的话指示器慢一轮**，看起来像"卡在报告模式"（此前审计里那条"注释说会自动 rerun 是错的"在此修正） |

### 关键行为事实（决定了网页上能看到什么）

**报告轮结束时模式一定已自动复位为 normal**（`chat_stream` 末尾 `if session["mode"]=="report": finish_report`），
所以侧边栏「📊 当前：报告模式」**只在轮次被中断时**才出现（模型报错、Streamlit Stop、浏览器刷新）。
正常跑完的报告轮，侧边栏看到的是 💬 普通模式 —— 这不是 bug。

### 现成的真实样本：`conversations.id=153`

- 标题「生成我的使用报告」，`mode='report'`，archived，created 10:44 / updated 10:47。
- 成因：该轮走到 `rag_summarize`（当时还是有 hnsw 报错）后中断，没产出最终回答 →
  `finish_report` 未执行 → 模式停在 report 落盘。
- 它的历史顺带证明了 P0-1 的回退机制生效：`fetch_external_data` 返回
  `{"实际月份":"2025-12","用户请求月份":"2026-09",...}`。
- **用途**：在页面上打开它 → 自检面板会显示「权威 report / 镜像 normal」，这正是修复前会
  判错提示词的分歧场景；重启 streamlit 后仍显示 report，即验证持久化。

### 会话库现状（测试残留，待用户决定是否清理）

`data/sessions.db` 中 user 1001：active 1 条、archived 116 条（含上面那条 report）。
大量是历次测试脚本（`react_agent.__main__`、集成测试）写入的残留，侧边栏历史列表会很长。
未擅自删除，等用户确认后再清。

### 无头渲染验证套路（可复用）

Streamlit 1.63 自带 `streamlit.testing.v1.AppTest`：

```python
from streamlit.testing.v1 import AppTest
at = AppTest.from_file("app.py", default_timeout=180)   # 预热知识库约 3~5s
at.run()
assert len(at.exception) == 0
[t.value for t in at.sidebar.markdown]   # 断言侧边栏指示器
[e.label for e in at.sidebar.expander]   # 断言自检面板存在
```

比 `streamlit run` + 手动点更可靠：能直接断言渲染出的元素，且不需要浏览器。
注意 `AppTest.from_file` 的相对路径以 cwd 为基准，必须在项目根运行。

---

## P0-3 核查：报告流程"被截断在第 2 轮"

### 用户交办的判据不成立（重要）

原判断题写"日志显示第 2 轮就收尾（回答长度 9）、`fetch_external_data` 从未被调用"。
实际查证：**带"回答长度 9"的那批日志是打桩/批量脚本的产物，不是真实模型行为**。

- `agent_20260923.log` 18:46:08 与 20:22:58 两条轨迹里，从"第 1 轮"到"报告完成 + 恢复普通模式"
  全部落在**同一秒内的 <200ms**；真实模型单轮至少数百毫秒到数秒。
- 更明显的形态证据：`18:46:08,657` 出现「用户 1003 已切换到报告模式」→ 紧接着 `18:46:08,667`
  「恢复普通模式」，**中间没有任何用户消息**；同秒内的 1004 问的是"每天用合适吗"却在 1 轮返回 9 字符。
  → 只有打桩/批量评测脚本才会产生这种形态。
- 真实模型的 5 个报告轮次**全部先调了 `fetch_external_data`**，无一轮跳过取数直接写报告：

| 时间 | 轨迹 | 结果 |
|---|---|---|
| 09-11 16:14:28 | fill_context(第2轮) | → 迭代 5 轮 / 2519 字 |
| 09-11 16:16:46 | fill_context(第1轮) | → 迭代 4 轮 / 4170 字 |
| 09-25 10:46:34 | get_current_month → fill_context(第2轮) → **fetch_external_data(第3轮)** → get_weather → rag_summarize | 被 403 中断 |
| 09-25 10:57:31 | get_user_id+get_current_month(第1轮) → fill_context(第2轮) → **fetch_external_data(第3轮)** → rag_summarize×2 | 迭代 5 轮 / 3743 字 |
| 09-25 11:09:50 | get_current_month(第1轮) → fill_context(第2轮) → **fetch_external_data(第3轮)** → rag_summarize | 迭代 5 轮 / 1637 字 |

### 但结构缺陷客观存在，已修

强约束（四步取数流程）**只写在 `main_prompt.txt`**，而 `fill_context_for_report` 一触发就切到
`report_prompt.txt` —— **约束恰好在这一刻消失**，而此时恰恰是数据还没取、最需要约束的时候。
`report_prompt.txt` 首句"根据查询回来的信息，写一份报告"在"历史里只有一条
`fill_context_for_report已调用`、没有任何数据"的语境下，确实容易被读成"信息已就绪"。
真实模型这两轮是靠 `main_prompt` 前两轮建立的意图惯性侥幸走对，不是提示词保证的。

### 改动：`prompts/report_prompt.txt` 重写

- 首句改为显式声明「进入报告模式不代表数据已就绪，必须先取数再写」。
- 新增「报告数据获取强约束」四步：`get_user_id` → `get_current_month` → `fetch_external_data`
  （**未拿到它的返回结果前禁止输出报告正文**）→ 可选 `rag_summarize`。
- 明确 `fill_context_for_report` 已在本轮触发，**不要重复调用**（原提示词仍描述该工具，
  模型有重复调用的风险）。
- 输出要求新增第 4 条：数据必须来自工具返回，工具没返回的指标不写、不估算。

### A/B 实测（真实模型，非打桩）

构造确定性"危险入口"：新建会话后先 `_switch_to_report_mode("1001", cid)`，再提问
「生成我的使用报告」——等价于"模型第 1 轮就调用了 fill_context_for_report"，历史里没有任何数据，
只有 report_prompt 在起作用。用临时库（`.workbuddy/_tmp_p3`），不污染 `data/sessions.db`。

| 版本 | 工具链 | 结果 |
|---|---|---|
| OLD（1260 字） | get_user_id+get_current_month+get_user_location → **fetch_external_data** → get_weather+rag_summarize×2 | 2282 字，声明"基于 2025-12"；开头多出英文行 `I'll gather the necessary information first.` |
| NEW（1228 字） | 同上 | 2327 字，声明"基于 2025-12"；开头干净 |

→ **两个版本都正确取数**，旧版并未复现"跳过取数"，所以这次属于**防御性加固**而非 bug 修复；
新版把"靠模型推断"变成"提示词显式要求"，并去掉了多余英文行。

回归：`test_react_integration.py`、`test_session_persist.py` 全部通过；
`load_report_prompts()` 能正常加载新文件（1228 字，含新约束段）。

### 注意点

- 提示词在 `ReactAgent.__init__` 加载，而实例被 `st.cache_resource` 缓存 →
  **改完提示词必须重启 streamlit 进程**，浏览器刷新无效。
- 判断日志里的行为是否来自真实模型，先用时间戳判形态：多轮挤在同一秒内 ⇒ 打桩/批量脚本。
  本项目里 `agent_20260911.log` 21:13:31、`agent_20260912.log` 20:33:07、`agent_20260917.log` 12:48:08
  都有同类"同秒多轮"记录，引用前务必先甄别。

---

## P0-5 修复：get_weather 在 lang_zh 为空列表时整体失效

### 用户报告的问题坐实（实测）

`agent/tools/agents_tools.py:128` 原写法 `current.get("lang_zh", [{}])[0]` —— **`get` 的默认值只在
key 缺失时生效**，key 存在但值为空列表时 `[0]` 照样 `IndexError`。取证结果：

| 场景 | 原表达式 |
|---|---|
| `lang_zh=[]`（真实会发生） | ❌ `IndexError: list index out of range` |
| `lang_zh` 缺失 | ✅ 回落 weatherDesc |
| `lang_zh=[{"value":""}]` | ✅ 回落 weatherDesc |
| `lang_zh=[{"value":"晴"}]` | ✅ 取中文 |

端到端打桩（走真实 `get_weather`）：
- 空 `lang_zh` → `城市测试城天气信息暂时无法获取`（**整条降级**，而同一份响应里
  `weatherDesc: [{"value":"Light rain"}]` 明明可用）
- 正常 → `城市测试城天气为小雨，气温18摄氏度，空气湿度72%，NE风11公里/小时`

日志同步出现 `[get_weather] 调用失败: list index out of range`（第 144 行 except 捕获）。
→ 属于**静默降级**：异常被吞，模型拿到"暂时无法获取"，无法察觉是解析 bug。

### 改动

1. `agents_tools.py::get_weather` 天气描述解析：
   ```python
   weather_desc = (
       (current.get("lang_zh") or [{}])[0].get("value")
       or (current.get("weatherDesc") or [{}])[0].get("value")
       or "未知"
   )
   ```
   顺手用同一 `or` 惯用法加固了 `weatherDesc` 兜底（同一行、同一 bug 类），
   两者都取不到时输出"未知"，不丢弃温度/湿度/风向等有效字段。附带注释说明
   `get(key, default)` 与 `x or default` 的区别。
2. `verify_tools.py::weather_check` **补强断言**（这是本次最值得记的一条）：
   原断言只查"结果含城市名"，而降级文案"城市深圳天气信息暂时无法获取"**同样含城市名**
   → 这个 bug 一直能静默通过项目自带的工具校验。现改为：
   禁止出现"暂时无法获取"/"天气为None"/"天气为未知"，并要求含"摄氏度"与"湿度"。

### 验证

- 7 个桩场景全过：空 lang_zh→英文兜底 / 正常→中文 / 空 value→英文 / key 缺失→英文 /
  两者皆无→"未知"但保留温度 / 带降雨预报 / 缓存命中（第二次不再请求，`_weather_cache` 未受影响）。
- 真实 API 端到端（北京、深圳）正常，降雨字段正常拼接。
- `verify_tools.py` 11 项全过；`test_react_integration.py`、`test_session_persist.py` 全过；
  `py_compile` 通过。

### 同类反模式全局排查

正则 `\.get\([^()]*,\s*\[[^\]]*\]\)\s*\[` 扫全项目，只命中 `middleware.py:156`
`params.get("user_id", [None])[0]` —— **同款写法但不会触发**：`parse_qs` 对存在的 key 永远
至少给一个值；实测 `?user_id=` 会被直接丢弃（key 缺失 → 走默认值 `[None]` → 由 `if not user_id`
兜住），`?user_id=1&user_id=2` 得 `["1","2"]`。无空列表情形，故**未改动**（避免无关改动）。

### 教训（可复用）

**校验脚本的断言必须能区分"成功"与"降级"**，否则等于没测。只断言"包含城市名/非空/无异常"
这类弱条件，会让"整条降级为兜底文案"的 bug 静默通过。

---

## 第二轮技术债核查（12 项，逐条取证）

用户一次交办 12 项，要求"检查是否存在 → 逐条改 → 每条给验证方案 → 等确认再下一项"。
本轮只做核查 + 第 1 项（git）。

### 核查结论总表

| # | 项 | 结论 | 关键证据 |
|---|---|---|---|
| 1 | git 冲突 / 未提交改动 | **存在**，形态与报告不同 | `MERGE_HEAD=1f55cfb`，README.md 是 `AA`；但**工作区已无冲突标记**（`grep -c '^<<<<<<<\|^=======$\|^>>>>>>>'` = 0），内容 = HEAD 版 + 9 行改进 → 早已手工解决、只是没 `git add`。未提交实为 **13 改 + 2 删**（非 8 个） |
| 2 | 跨用户切换遗留孤儿会话 | **成立**（代码路径确凿，当前无实例） | `start_new_conversation(user_id)` 只 `archive_all_active(user_id)`；`list_archived` 只查 `status='archived'` → 旧用户 active 会话不可见且不清理。实测当前库只有 1 条 active（user 1001），未出现并发 |
| 3 | 悬空 tool_calls | **代码缺陷成立**，DB 里的 25 例是脚本产物 | `react_agent.py:360-361` 先 append `AIMessage(tool_calls)` 并立即落盘，**之后**才在 384-389 逐条 append `ToolMessage`。`_init_session` 恢复时无任何清洗。DB 中 25 条悬空会话全是 id 75~99、`updated_at` **同一秒** `2026-09-25T10:44:18`、每条恰好 2 条消息 → 批量脚本形态，非真实崩溃 |
| 4 | 首次 RAG 阻塞 28 秒 | **已修（上一轮）**，但代价是"挪走"不是"消除" | 预热已挪到 app 启动（`warm_up_knowledge_base` + `st.spinner`）。实测首次 `_get_rag()` **38.56s**；拆解：`import rag.vector_store` 8.41s、embedding 模型 0.73s、`VectorStoreService()` 0.14s、首次检索 1.1s。日志 6 个文件全部"已存在跳过" → **这几十秒主要不是入库，而是模块导入 + 客户端初始化** |
| 5 | `save_conversation` 全量重写 | **成立** | `session_store.py:317-326`：`DELETE FROM conv_messages WHERE conversation_id=?` 后 `executemany INSERT` 全量重插。当前最大会话 22 条消息、消息总行数 559，规模尚小但复杂度 O(n²) |
| 6a | `pending_switch_user` 死代码 | **成立** | 全项目 grep 仅 3 处：`app.py:41`（注释）+ `47/48`（**读**）。**无任何写入点** → `if` 分支永不进入，注释描述的"点击历史会话时只写入"与实际实现不符 |
| 6b | `app.py:241` 注释"输入提交后会自动 rerun" | **已修（上一轮）** | 现在 `app.py:296-299` 注释已改为"本轮结束后立即 rerun"，并在对话收尾补了 `st.rerun()`。**已不存在** |
| 7a | `_rag_cache`/`_weather_cache` 无上限 | **成立** | `agents_tools.py:44/61` 都是裸 `dict`，只有 TTL 判断，无容量上限、无淘汰（TTL 过期条目也留在字典里，永不删除） |
| 7b | 日志无轮转、单日 103 KB | **成立** | `utils/logger_handler.py:45` 用 `logging.FileHandler`（非 `RotatingFileHandler`）；实测 `agent_20260917.log` = 103085 B、`agent_20260925.log` = 99891 B，按天分文件但单文件可无限增长 |
| 7c | `_next_6h_rain_desc` 用 `>= now_hour` 漏当前时段 | **成立，且比报告描述的更严重** | 3 小时粒度实测（时段起点 0/3/6/…/21）：**now=04:30 → 输出 40%**。`>=4` 取到的是 06:00 段(30%) 与 **09:00 段(40%)**，即窗口变成 06:00–11:59——既漏掉进行中的 03:00 段(20%)，又**取到了 6 小时窗口之外的数据**。now=22:30 且响应只有 1 天时返回 `""`（真实 API 有 2~3 天，暂不致命） |
| 7d | requirements.txt 缺 `dashscope`、未锁版本 | **成立** | `model/factory.py:36` 用 `langchain_community.embeddings.DashScopeEmbeddings` → 运行时**必需** `dashscope` 包，但 requirements.txt 只在注释里提了一句；全部依赖均为 `>=` 未锁版本 |
| 7e | `config/rag.local.yml` 的 `qwen3.8-max` 是否有效 | **前提不成立** | `config/` 下只有 agent/chroma/prompts/rag 四个 yml，**不存在 `rag.local.yml`**；`qwen3.8-max` 写在 `config/rag.yml` 里，配套端点是私有 MaaS（`ws-<workspace-id>.cn-beijing.maas.aliyuncs.com/compatible-mode/v1`），私有端点的模型名可以是自定义的，且 verify_tools 里模型能正常初始化 |

### 第 1 项已执行

`git add README.md` → `git commit --no-edit`，产出合并提交 **`a51a2c0`**（双亲：`515c9ab` 本地首次提交 + `1f55cfb` 远端 Initial commit），`.git/MERGE_HEAD` 已清除。
暂存内容仅 README.md（5 insertions / 4 deletions），**13 个已跟踪文件的修改仍留在工作区未提交**（有意为之，避免把待修的代码一起提交）。

### 第 1 项用户复核（2026-09-25 16:47）

用户执行复核命令时把 `--oneline` 误打成 `--online`，报 `fatal: unrecognized argument` —— 不是仓库问题。
重新核对确认第 1 项闭环：`MERGE_HEAD` 已清除、`git diff --name-only --diff-filter=U` 为空、
README.md 工作区干净且冲突标记数 0、`git rev-list --left-right --count origin/main...main` = `0	2`（本地领先 2，远端无独有提交 → 可直接 fast-forward push）。

#### ⚠️ 提交基线前必须先处理：真实端点写在**被跟踪**的 `config/rag.yml` 里

- `.gitignore` 只忽略 `config/rag.local.yml`，**`config/rag.yml` 是被跟踪的**（`git ls-files config/` 可见）。
- 工作区把 `config/rag.yml` 从模板改成了真实配置（`api_base` 填了私有 MaaS 端点 `https://ws-<workspace-id>.cn-beijing.maas.aliyuncs.com/compatible-mode/v1`，
  并把 `chat_model_name` / `embedding_model_name` 换成私有部署的自定义模型名），并**删除了 `config/rag.yml.example`**。
  （此处刻意不写全端点，避免工作记忆本身成为泄露源。）
- 但同一批改动里的 `utils/config_handler.py` 新实现的正是"`rag.yml` 做基线 + `rag.local.yml` 覆盖"，
  且 .gitignore 注释写着"仓库内只保留 rag.yml.example 模板" → **工作区状态与新代码/文档的设计意图相反**。
- 后果：若 `git add -A` 后 push，私有 MaaS 端点与自定义模型名会进入公开仓库历史（不可逆）。
- 正确形态：`config/rag.yml` 回退为占位模板（`api_base: ""`）→ 真实值移入 `config/rag.local.yml`（已被忽略）→ 恢复 `config/rag.yml.example`。
  运行时行为不变（config_handler 先读 rag.yml 再被 rag.local.yml 覆盖）。

### 环境补充事实

- `git log --all --graph` 看不到 `1f55cfb` —— 该提交不在任何 ref 上，说明当初是直接 `git merge <sha>` 引入的。
- 本机 **Bash 工具现在可用**（`ls`/`rm`/`grep`/`diff`/`wc` 均正常），且 stdout 会正常返回。
  这与跨项目记忆里"Bash 不可用、统一改用 PowerShell"的旧结论**冲突，以实测为准**：
  读输出优先用 Bash（PowerShell 的 stdout 不返回）。

---

## 配置归位 + 提交基线（16:47~16:52）

### config 分层归位

用户确认采用「拆到 rag.local.yml」方案：
- `git checkout HEAD -- config/rag.yml config/rag.yml.example` 把两个文件还原成占位模板（`api_base: ""`、`qwen-max`）；
- 新建 `config/rag.local.yml` 存真实的私有端点与自定义模型名（`qwen3.8-max` / `qwen3.7-text-embedding`），
  该文件被 `.gitignore:65` 忽略；
- 实测 `load_rag_config()` 生效值仍为私有端点与自定义模型名 → **运行时行为零变化**。

### 记忆文件脱敏

工作记忆里 3 处写有私有端点明文（`2026-09-23.md` 1 处、`2026-09-25.md` 2 处），全部改写为
`ws-<workspace-id>` 形态。脱敏后全仓库搜索该端点仅命中 `config/rag.local.yml`（已忽略）。
→ 教训：**工作记忆也会被提交**，写记忆时就不要抄真实端点/密钥。

### 两个基线提交

- `7c78b58` chore(memory): 同步工作记忆文件（删 09-17 日志、加 09-23/09-25 与 MEMORY.md）
- `897628b` fix: 修复检索自愈、报告模式权威源、天气解析越界等问题（11 个文件，+427/-95）

每个提交前都核对 `git diff --cached --stat`，只用显式路径 `git add`，**没有用 `git add -A`**
（避免把 `_verify_*.py`、`eval_*`、审计报告等未跟踪文件一把带进来）。提交后工作区已跟踪文件干净。

---

## P2：会话历史悬空 tool_calls（16:52~16:58 已修）

### 机理（实测坐实）

`AIMessage(tool_calls)` 先落盘、配对的 `ToolMessage` 后落盘，中途中断即留下缺配对响应的历史。
**真模型 A/B 证据**（`_verify_p2.py` C 组，走私有 MaaS 真实端点）：

| 输入历史 | 结果 |
|---|---|
| 悬空（声明 1 个 tool_call、0 个响应） | ❌ `OpenAIInvalidRequestError 400 invalid_parameter_error`：`Can only get item pairs from a mapping.` |
| 同一条清洗后 | ✅ 正常返回 |
| 部分配对（3 个调用只回 1 个） | ⚠️ 本端点**容忍**、未报 400（但仍是非法形态，清洗器一并修正） |

→ 结论要说准：**"持续 400" 只对"完全悬空"成立**，而"完全悬空"恰是危险窗口最大的形态
（窗口 = AI 消息落盘后到第一个工具返回前，RAG 类工具几十秒）。

### 两层修复

1. **写入原子化**（`agent/react_agent.py`，`chat()` 与 `chat_stream()` 各一处）：
   改为「先执行完全部工具并收集配对响应 → 再把 AI 消息 + 全部 tool 响应一次性 append 并落盘」。
   工具执行期间历史末尾仍是用户消息，所以 `_switch_to_report_mode` 此刻的落盘也合法。
2. **恢复时清洗**（`agent/tools/session_store.py`）：新增公开函数 `sanitize_history()`（幂等），
   在 `get_conversation()` 里统一调用。规则：无配对响应的 tool_call 从声明中剔除 →
   剔除后既无 tool_call 又无正文的 AI 消息整条丢弃 → 孤儿 tool 消息丢弃。
   **旧数据自动自愈，不需要一次性迁移**：实测真实库 25 条悬空会话（id 75~99，全部 user 1001）
   读取后悬空数归零。

### 验证结果

`_verify_p2.py`（结果 `_p2_report.json`）：
- A 单元 8/8：尾部悬空（有无正文两种）、完整配对不受影响、部分配对、孤儿 tool、多轮中间被打断、幂等
- B：真实库 25 条 → 读取后 0 条悬空
- C：真模型 A/B（见上表）
- D：`chat()` 崩溃模拟 —— 第 2 个工具抛 `KeyboardInterrupt`（`BaseException`，不被 `except Exception` 吞），
  中断后落盘历史 = `["human"]`、无悬空、**新进程续问正常**
- D2：`chat_stream()` 同样场景 —— 同上全部通过

回归：`test_react_integration.py`、`test_session_persist.py` 全过；`py_compile` 通过。

### 教训（重要）

**项目自带的两个回归测试完全不覆盖 `chat_stream`**（grep `chat_stream` 在两个 test 文件里零命中）。
这次改动同时动了流式路径，只能自己补 D2 用例。以后改 `chat_stream` 必须单独验证。

### 页面级复现入口

`data/sessions.db` 里 user 1001 的历史会话「**首次使用扫地机器人需要做什么？**」（id=75）就是
一条悬空记录（AI 声明了 `rag_summarize`、没有响应）。**修复前**打开它再提问会直接 400；
**修复后**可以正常继续对话 —— 这是页面上唯一能直接复现这条 bug 的样本。

> 注：这 25 条的 AI 正文其实是有实质内容的（不是空壳），形态更像"批量预置脚本被中断"，
> 而非纯打桩空跑。审计里"每条恰好 2 条消息"的判据成立，但"内容无意义"的推断不成立。

### ⚠️ 环境事实：后台启动的 streamlit 实例会在本轮结束时被回收

本轮用 `run_in_background` 起 8502，启动成功（`Uvicorn server started on 127.0.0.1:8502`、
HTTP 200）并**正常服务了整个本轮**，但本轮结束后进程即被终止（Duration 2m1s，status=failed）。
→ **`present_files` 给出的页面链接只在当轮有效**。需要用户在自己终端启动页面做人工验证时，
必须把命令给出来，不要假定链接还活着：

```
cd D:\Agent
C:\Users\21301\AppData\Local\Programs\Python\Python311\python.exe -m streamlit run app.py --server.port 8502
```

---

## 会话库清理（2026-09-25 17:07）

用户确认后执行。**只删 `status='archived'` 的记录**，保留当前 active 空会话（id=164）——
它被运行中的页面引用着，删掉会让后续 `save_conversation` 在 `SELECT ... WHERE id=?` 拿不到行、
走 `return` 分支**静默丢弃消息**（`session_store.py` 的字面行为，不是猜测）。

| | 清理前 | 清理后 |
|---|---|---|
| conversations | 118 | 1 |
| conv_messages | 559 | 0 |
| archived | 117（id 41~163） | 0 |
| 悬空 tool_calls 会话 | 25 | 0 |
| 孤儿消息 | — | 0 |

- 全量备份（**清理前**状态，118 会话 / 559 消息 / 117 archived）：
  `.workbuddy/backups/sessions.db.20260925_170729.pre-cleanup.bak`
- ⚠️ 备份要用 sqlite 的 `VACUUM INTO`，**别直接 `cp`**：WAL 模式下已提交但未 checkpoint 的页
  可能还躺在 `-wal` 里，`cp` 只拷主文件会丢数据（本次恰好没丢，但不能指望）。
  另：用 `mode=ro` 打开备份文件会**顺手生成 `-shm`/`-wal`**，备份目录因此变脏，记得清掉。
- `.gitignore` 已补 `.workbuddy/backups/` 与 `.workbuddy/_*`（备份含用户对话数据，禁止提交）。
- 清理后 25 条悬空记录**一条不留**，因此"读取时自愈"这条能力在真实库上暂时没有样本可验了；
  要复现请用 `_verify_p2.py` 的 D 组自造。

## 第 3 项：跨用户孤儿 active 会话（2026-09-25 已修）

**问题链路**：`start_new_conversation(user_id)` → `archive_all_active(user_id)` 只归档当前用户；
而历史列表 `list_archived(user_id)` 只查 `status='archived'`。
→ A 用户切到 B 用户后，A 的会话仍是 `active` 且属主不在筛选范围内 = **既不出现在历史列表、
也不会被清理**，对话内容永久不可见（浏览器中途关闭同理）。

**修法**：让"同一时刻至多一条 active"成为不变量。
- `session_store.archive_all_active(user_id=None)`：`None` 时归档**全部用户**的 active；
  传 `user_id` 时保持原语义（`test_react_integration.py:85` 仍在用，向后兼容）。
- `app.py::start_new_conversation` 改为无参调用。

**代价（如实记录）**：多浏览器场景下，B 标签页打开页面会把 A 标签页正在进行的会话一并归档。
不会丢数据（消息仍按 conversation_id 追加，会话照旧出现在 A 的历史列表），只是可能提前收起。
本应用是单用户演示定位，接受此代价换取"孤儿不可能存在"。

**验证脚本**：根目录 `_verify_p3.py`（A 语义 4 例 / B 旧行为vs新行为对照 / C app 调用点静态检查 /
D 真实库只读体检），报告 `_p3_report.json`。B 组是关键：同一份数据先跑旧调用
`archive_all_active("1002")` 复现出孤儿（`active` 且历史列表 0 条），再跑新调用
`archive_all_active()` 得到 `archived` 且历史列表可见、消息数 2、全局 active=1。

### 分提交的一个技巧（可复用）

P2 与 P3 都改了 `session_store.py`，无法靠 `git add <path>` 拆开，`git add -p` 又是交互式。
做法：**先把文件备份到 gitignore 覆盖的目录 → 手工还原后一项的 hunk → 提交前一项 → 拷回 → 提交后一项**。
这样每个提交都自洽（不会出现"提交里 app.py 已改但 session_store 还是旧签名"的坏中间态）。
本次：`789e307`（P2 悬空 tool_calls）+ `c435c88`（P3 跨用户孤儿）。

### ⚠️ 环境事实：用户自己在跑页面实例

17:02:59 用户手动在 8502 起了 streamlit（跑的是 P2 代码）。**不要擅自杀用户的进程**，
也不要对它共享的 `data/sessions.db` 做破坏性操作（例如跑 AppTest —— 它会
`archive_all_active()` 把用户会话删掉/新建）。需要页面验证时把命令给用户，让他们自己重启。


